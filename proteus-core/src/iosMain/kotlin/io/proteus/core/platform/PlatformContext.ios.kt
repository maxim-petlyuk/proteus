package io.proteus.core.platform

actual abstract class PlatformContext {
    // For iOS, we might not need specific context initially
    // This can be expanded later with UIViewController or other iOS-specific needs
}